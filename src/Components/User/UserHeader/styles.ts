import styled from 'styled-components';

const Container = styled.header`

  display: grid;
  grid-template-columns: 1fr auto;
  align-items: center;
  margin-top: 1rem;
  position: relative;

`;

export {Container};
